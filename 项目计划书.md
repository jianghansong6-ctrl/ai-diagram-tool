# AI 科研机制图绘制工具 — 项目计划书

---

## 一、项目对象

本项目面向需要绘制专业科研示意图的**科研人员、高校教师、研究生及工程技术人员**，具体包括：

- **论文发表场景**：科研人员在撰写期刊论文、会议论文时，需要绘制神经网络结构图、信号通路图、系统架构图、实验流程图等专业示意图。
- **学术汇报场景**：研究生和教师在组会汇报、学术讲座、课题答辩中，需要制作清晰直观的幻灯片示意图。
- **教学备课场景**：高校教师在课程教学中，需要绘制用于讲解复杂概念的教学示意图和知识框架图。
- **技术文档场景**：工程技术人员在撰写专利文档、技术白皮书、项目申报材料时，需要绘制系统架构图和技术原理图。

项目旨在替代传统手动绘图工具（如 PowerPoint、Adobe Illustrator、Visio），提供一套基于 AI 的、交互式的、覆盖多类型科研图表的智能绘制解决方案。

---

## 二、项目背景和意义

### 2.1 行业背景

在科研论文、学术报告和技术文档中，高质量的机制图和示意图是传递复杂概念的核心载体。一张清晰的流程图、框架图或数据图表，胜过千言万语。然而，当前科研绘图领域面临以下痛点：

**（1）工具门槛高**
传统科研绘图依赖 Adobe Illustrator、Inkscape、Microsoft Visio、PowerPoint 等通用设计软件。这些软件功能丰富但操作复杂，需要用户具备一定的设计能力和软件操作经验，学习曲线陡峭。据调查，科研人员平均需要花费 3-5 小时才能完成一张中等复杂度的机制图。

**（2）修改迭代困难**
科研过程中图表需要反复修改——调整布局、增减元素、更新数据。手动绘图每次修改都意味着大量重复劳动，甚至需要重绘。版本管理混乱，不同版本的图表难以追溯。

**（3）AI 绘图工具对科研场景支持不足**
现有 AI 图像生成工具（如 Midjourney、DALL·E、Stable Diffusion）多聚焦于自然图像和艺术创作，对科研领域所需的**结构化示意图**（流程图、框架图、思维导图、统计图表等）支持不足。这些工具生成的是像素图像而非可编辑的结构化元素，无法保证逻辑清晰、布局合理的专业图形输出。

**（4）缺乏交互式生成能力**
大多数 AI 生成工具采用"一次性生成"模式，用户无法在生成过程中干预、暂停、修改局部元素或微调布局。生成结果不满意只能调整提示词重新生成，效率低下且不可控。

**（5）多格式输出需求**
科研图表需要适应多种发布场景：论文插图需要高分辨率位图或矢量图（PNG/SVG），会议演示需要可编辑的 PowerPoint 文件（PPTX），网页展示需要矢量图形。现有工具往往只支持单一导出格式，科研人员需要手动进行格式转换。

### 2.2 技术发展趋势

近年来，大语言模型（LLM）在自然语言理解和代码生成方面取得了突破性进展。GPT-4o、Claude 等模型不仅能理解和生成自然语言，还能输出结构化的指令序列。与此同时，SSE（Server-Sent Events）流式传输技术、HTML5 Canvas 渲染引擎、OCR 文字识别技术的成熟，为构建新一代智能绘图工具提供了技术基础。

### 2.3 项目意义

**（1）降低科研绘图门槛，提升研究效率**
用户只需用自然语言描述想要的图表内容，AI 即可自动理解意图并生成结构化绘图指令，实时逐元素渲染到画布上。从描述"Transformer 架构"到生成完整的神经网络结构图，仅需数十秒，极大降低了科研人员的绘图时间成本和技能门槛。

**（2）流式生成与实时交互的创新范式**
项目采用 SSE 流式传输技术，用户在图表逐步绘制过程中即可看到结果，并支持暂停/恢复、局部修改、批量修改、拖拽调整、撤销等交互操作。这种"边生成、边交互、边修改"的模式，突破了传统 AI 图像生成"黑箱式"的一次性输出局限。

**（3）覆盖多类型科研图表场景**
系统支持流程图、框架图、逻辑图、思维导图、表格、柱状图、折线图、饼状图等 8 种图表类型，覆盖科研全场景需求，从文字描述到数据可视化一应俱全。

**（4）文件解析与 OCR 赋能数据驱动绘图**
支持上传 PDF、Word、TXT 及图片文件，自动提取文本内容。图片 OCR 功能支持中英文识别，可将论文截图、扫描件中的文字信息直接转化为可编辑的图表，打通从文献阅读到图表生成的完整链路。

**（5）推动 AI for Science 工具实践**
在人工智能赋能科学研究的大背景下，本项目代表了"AI for Science"在科研工具领域的具体实践，将大语言模型的语义理解能力与结构化的图形渲染引擎相结合，为科研人员提供了一种全新的、智能化的学术表达方式。

---

## 三、拟采用方案

### 3.1 系统架构设计

采用前后端分离的 B/S 架构，前端负责交互界面与图形渲染，后端负责 AI 生成逻辑与数据处理。

```
┌────────────────────────────────────────────────────────────┐
│                        浏览器端                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │历史记录  │  │  Canvas  │  │提示词输入│  │控制面板  │  │
│  │  侧边栏  │  │  画布    │  │ 文件上传 │  │导出按钮  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                      ↕ HTTP / SSE                          │
├────────────────────────────────────────────────────────────┤
│                       FastAPI 后端                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │会话管理  │  │ LLM 客户端│  │文件解析  │  │布局优化  │  │
│  │  │       │  │          │  │  OCR     │  │          │  │
│  ├──────────┤  ├──────────┤  ├──────────┤  ├──────────┤  │
│  │数据库    │  │提示词模板│  │格式导出  │  │导出器    │  │
│  │(SQLite)  │  │          │  │PPTX/SVG  │  │          │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                      ↕ API                                  │
│               OpenAI / Anthropic LLM API                    │
└────────────────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| **前端框架** | React 18 + Vite 5 | 组件化开发，热更新调试，构建性能优异 |
| **图形渲染** | HTML5 Canvas 2D | 原生高性能 2D 绘图 API，支持逐元素动画渲染 |
| **后端框架** | Python FastAPI | 高性能异步 Web 框架，原生支持 SSE 流式推送 |
| **AI 模型** | GPT-4o / Claude | 业界领先的大语言模型，支持流式输出结构化指令 |
| **文件解析** | PyMuPDF / python-docx / pytesseract | PDF、Word 文档解析，图片 OCR 文字识别 |
| **数据存储** | SQLite | 零配置嵌入式数据库，满足单机部署需求 |
| **格式导出** | python-pptx / 自定义 SVG 生成 | 支持可编辑 PPTX 和矢量 SVG 两种主流格式 |

### 3.3 核心技术方案

#### 3.3.1 AI 指令化绘图

采用"自然语言 → 结构化指令 → Canvas 渲染"的三层转换方案：

1. **提示词工程**：针对每种图表类型设计专门的系统提示词，包含布局规则、配色方案、视觉语法约定，引导 LLM 输出规范的 JSON 指令序列。
2. **流式传输**：SSE 将 LLM 的输出实时推送到前端，前端每收到一条指令立即渲染，实现"边生成边绘制"的流畅体验。
3. **指令格式**：统一的绘图指令格式 `{ action, params, id, description }`，覆盖矩形、圆形、椭圆、箭头、线条、文本、标签、扇形等 12 种绘图动作。

#### 3.3.2 交互式画布引擎

基于 HTML5 Canvas 实现的交互式渲染引擎，具备：

- **命中检测**：边界框碰撞检测算法，支持点击选中元素
- **框选功能**：矩形框选区域，批量选中多个元素
- **拖拽移动**：元素自由拖拽，关联箭头智能跟随重算端点
- **渐入动画**：新元素生成时播放渐入动画，提升视觉体验
- **高分辨率导出**：3 倍缩放离屏 Canvas 渲染，导出高清 PNG

#### 3.3.3 后端布局优化器

AI 生成结果经过后处理优化管线：

1. **文本自适应**：根据文字内容动态调整形状包围盒大小
2. **行对齐**：基于纵坐标的行分组与水平对齐
3. **重叠检测**：全局 3 遍扫描解决元素重叠
4. **箭头吸附**：自动计算箭头端点至形状边缘的最短连接
5. **画布居中**：整体内容在画布中居中排列

#### 3.3.4 文件解析与 OCR

支持多格式文件输入：

- **PDF**：PyMuPDF 提取文本内容
- **DOCX**：python-docx 提取段落文本
- **TXT**：UTF-8 文本解码
- **图片**：pytesseract 引擎，中英文 OCR 识别，降级返回图像元信息

#### 3.3.5 多格式导出

- **PPTX**：使用 python-pptx 库，保留形状、颜色、文本、箭头为可编辑元素
- **SVG**：自定义 XML 生成，含渐变、阴影、文字阴影等出版级效果
- **PNG**：Canvas toBlob 导出，3 倍缩放保证印刷分辨率

### 3.4 功能模块

| 模块 | 功能 |
|------|------|
| 图表生成 | 自然语言输入 → AI 流式生成 → 实时 Canvas 渲染 |
| 图表类型 | 流程图、框架图、逻辑图、思维导图、表格、柱状图、折线图、饼状图 |
| 交互编辑 | 点击选中、框选、拖拽、局部修改、批量修改、删除、撤销 |
| 文件输入 | PDF/DOCX/TXT 解析 + 图片 OCR |
| 数据检测 | 智能识别文本中数值数据，启用对应图表类型 |
| 会话控制 | 暂停/恢复/停止生成、进度条显示 |
| 历史管理 | SQLite 持久化 + 侧边栏历史浏览 + 加载历史 |
| 格式导出 | PPTX 可编辑文件、SVG 矢量图、PNG 高清位图 |
| 多语言 | 中文/英文界面切换，AI 生成内容自动适配语言 |

---

## 四、预期效果

### 4.1 直接效果

**（1）绘图效率显著提升**
用户输入一段文字描述，AI 在 10-30 秒内即可生成一张结构完整、布局合理的科研示意图，相比传统手动绘图（3-5 小时）效率提升 **数十倍**。

**（2）图表质量达到出版标准**
布局优化器确保元素对齐、间距均匀、无重叠；SVG/PNG 导出满足学术期刊对插图分辨率（300 DPI 以上）和格式的要求；PPTX 导出保留完全可编辑性。

**（3）交互体验流畅自然**
流式渲染让用户无需等待全部生成完成即可看到结果；生成过程中的暂停/恢复、生成后的局部修改和拖拽调整，使用户对最终结果拥有完全控制权。

**（4）支持完整的工作流**
从文件上传（PDF/Word/图片）→ 自动提取文字 → AI 生成图表 → 交互编辑优化 → 多格式导出，覆盖科研图表制作的完整流程。

### 4.2 应用效果

- **科研论文**：快速绘制神经网络架构图、实验流程图、系统框架图
- **学术报告**：生成逻辑清晰的思维导图、机制说明图
- **教学课件**：制作用于课堂教学的概念图、原理示意图
- **数据分析**：将实验数据自动可视化为柱状图、折线图、饼状图
- **技术文档**：绘制系统架构图、技术路线图、专利示意图

### 4.3 创新点总结

1. **方法创新**：提出了"自然语言 → 结构化指令 → Canvas 渲染"的三层转换方案，将 LLM 的语义理解能力与图形渲染引擎有机结合。
2. **技术创新**：基于 SSE 的流式生成 + 实时 Canvas 渲染，实现了"边生成、边绘制、边交互"的新型绘图范式。
3. **应用创新**：集成文件解析、OCR 识别、布局优化、多格式导出等能力，构建了从文献输入到图表产出的完整工具链。
4. **交互创新**：支持生成过程中的暂停/恢复控制、生成后的元素级局部修改、拖拽调整，突破了传统 AI 图像生成"一次性"的局限。

---

*本项目计划书由 SAM3（AI 科研机制图绘制工具）团队编制*
